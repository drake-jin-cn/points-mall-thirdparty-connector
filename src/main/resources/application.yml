server:
  port: ${PORT:8084}

spring:
  application:
    name: points-mall-thirdparty-connector
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  # WebFlux 默认使用 Netty 作为服务器（非阻塞），无需额外配置

app:
  jwt:
    secret: ${JWT_SECRET:dev-insecure-secret-at-least-32-chars!!}
